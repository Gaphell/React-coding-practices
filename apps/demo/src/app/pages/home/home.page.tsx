import React from 'react';
import WithLoader from '../../shared/components/loader/loader.container';

const HomePage = () => {
    return (
        <p>Hello</p>);
}

export default WithLoader(HomePage);